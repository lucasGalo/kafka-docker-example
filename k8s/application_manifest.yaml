apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kafka-argocr
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/lucasGalo/kafka-docker-example.git'
    targetRevision: main
    path: k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: kafka
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
